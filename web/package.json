{
  "devDependencies": {
    "@types/node": "^24.3.0",
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "dev": "deno run -A --unstable-raw-imports build.ts && vitepress dev",
    "build": "deno run -A --unstable-raw-imports build.ts && vitepress build",
    "preview": "vitepress preview"
  }
}
